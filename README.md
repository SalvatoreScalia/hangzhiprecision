# hangzhiprecision
Logs DC voltage, current, power and energy/Ah from PSM2000-Z devices using the HZP v3.9 protocol; performs grouped page reads to minimize latency, applies software offsets so counters start at zero, and writes time‑stamped snapshots to a semicolon CSV. Optional support to launch a parallel test script.
